# Radio 单选框

单选框组件用于在一组选项中选择单个值。

## 功能介绍

- 🎨 支持多种尺寸、颜色和变体
- 🔧 提供完整的表单验证支持
- ⚙️ 支持受控/非受控模式
- 📱 响应式设计，适配多端
- ♿ 无障碍支持，符合WAI-ARIA标准
- 🎭 丰富的动画效果

## 基本用法

```jsx
import React, { useState } from 'react';
import { Radio, RadioGroup } from '@taro-uno/components';

export default () => {
  const [value, setValue] = useState<string | number>('1');

  return (
    <RadioGroup
      value={value}
      onChange={setValue}
    >
      <Radio value="1" label="选项一" />
      <Radio value="2" label="选项二" />
      <Radio value="3" label="选项三" />
    </RadioGroup>
  );
};
```

## 使用示例

### 基础单选框

```jsx
<Radio value="1" label="单选框" />
```

### 单选框组

```jsx
import { useState } from 'react';
import { Radio, RadioGroup } from '@taro-uno/components';

const App = () => {
  const [value, setValue] = useState('1');

  return (
    <RadioGroup value={value} onChange={setValue}>
      <Radio value="1" label="选项一" />
      <Radio value="2" label="选项二" />
      <Radio value="3" label="选项三" />
    </RadioGroup>
  );
};
```

### 不同尺寸

```jsx
<RadioGroup>
  <Radio value="xs" size="xs" label="极小" />
  <Radio value="sm" size="sm" label="小" />
  <Radio value="md" size="md" label="中" />
  <Radio value="lg" size="lg" label="大" />
  <Radio value="xl" size="xl" label="极大" />
</RadioGroup>
```

### 不同颜色

```jsx
<RadioGroup>
  <Radio value="primary" color="primary" label="主要" />
  <Radio value="secondary" color="secondary" label="次要" />
  <Radio value="success" color="success" label="成功" />
  <Radio value="warning" color="warning" label="警告" />
  <Radio value="error" color="error" label="错误" />
  <Radio value="info" color="info" label="信息" />
</RadioGroup>
```

### 垂直布局

```jsx
<RadioGroup direction="vertical">
  <Radio value="1" label="选项一" />
  <Radio value="2" label="选项二" />
  <Radio value="3" label="选项三" />
</RadioGroup>
```

### 禁用状态

```jsx
<RadioGroup>
  <Radio value="1" label="可用" />
  <Radio value="2" disabled label="禁用" />
</RadioGroup>
```

### 错误状态

```jsx
<Radio status="error" label="错误状态" />
```

### 验证示例

```jsx
import { useState } from 'react';
import { Radio, RadioGroup } from '@taro-uno/components';

const App = () => {
  const [value, setValue] = useState('');
  const [error, setError] = useState('');

  const validate = () => {
    if (!value) {
      setError('请选择一个选项');
      return false;
    }
    setError('');
    return true;
  };

  return (
    <>
      <RadioGroup value={value} onChange={setValue}>
        <Radio value="1" label="选项一" />
        <Radio value="2" label="选项二" />
      </RadioGroup>
      {error && <div style={{ color: 'red' }}>{error}</div>}
      <button onClick={validate}>提交</button>
    </>
  );
};
```

### 自定义样式

```jsx
<Radio 
  value="custom" 
  label="自定义样式" 
  style={{ backgroundColor: '#f0f0f0', padding: '5px 10px', borderRadius: '4px' }}
  labelStyle={{ fontWeight: 'bold', color: '#333' }}
/>
```

### 带图标

```jsx
<Radio 
  value="icon" 
  label="带图标" 
  checkedIcon={<View style={{ color: 'red' }}>✓</View>}
  uncheckedIcon={<View style={{ color: 'gray' }}>○</View>}
/>
```

## API

### RadioProps

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| value | `string \| number` | - | 单选框值 |
| checked | `boolean` | `false` | 是否选中 |
| defaultChecked | `boolean` | `false` | 默认选中状态（非受控模式） |
| size | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | 单选框尺寸 |
| status | `'normal' \| 'error' \| 'warning' \| 'success' \| 'disabled'` | `'normal'` | 单选框状态 |
| variant | `'default' \| 'filled' \| 'outlined'` | `'default'` | 单选框变体 |
| color | `'primary' \| 'secondary' \| 'success' \| 'warning' \| 'error' \| 'info'` | `'primary'` | 单选框颜色主题 |
| disabled | `boolean` | `false` | 是否禁用 |
| readonly | `boolean` | `false` | 是否只读 |
| label | `ReactNode` | - | 标签文本 |
| labelPosition | `'left' \| 'right'` | `'right'` | 标签位置 |
| helperText | `ReactNode` | - | 辅助文本 |
| errorText | `ReactNode` | - | 错误文本 |
| bordered | `boolean` | `false` | 是否显示边框 |
| rounded | `boolean` | `false` | 是否圆角 |
| icon | `ReactNode` | - | 自定义图标 |
| checkedIcon | `ReactNode` | - | 选中时的图标 |
| uncheckedIcon | `ReactNode` | - | 未选中时的图标 |
| className | `string` | - | 自定义样式类名 |
| onChange | `(checked: boolean, event: ITouchEvent) => void` | - | 变化事件处理函数 |
| onClick | `(event: ITouchEvent) => void` | - | 点击事件处理函数 |
| style | `React.CSSProperties` | - | 自定义样式 |
| accessible | `boolean` | `true` | 是否启用无障碍访问 |
| accessibilityLabel | `string` | - | 无障碍标签 |
| accessibilityRole | `string` | - | 无障碍角色 |
| accessibilityState | `{ disabled?: boolean; readonly?: boolean; checked?: boolean; busy?: boolean; expanded?: boolean; }` | - | 无障碍状态 |
| rules | `Array<{ required?: boolean; message?: string; validator?: (checked: boolean) => boolean \| string \| Promise<boolean \| string>; }>` | - | 表单验证规则 |
| validateTrigger | `'onChange' \| 'onBlur' \| 'onSubmit'` | `'onChange'` | 验证触发时机 |
| immediate | `boolean` | `false` | 是否立即验证 |
| validator | `(checked: boolean) => boolean \| string \| Promise<boolean \| string>` | - | 自定义验证函数 |
| animation | `boolean` | `true` | 动画效果 |
| animationDuration | `number` | `300` | 动画时长 |
| ripple | `boolean` | `true` | 涟漪效果 |
| rippleColor | `string` | - | 涟漪颜色 |
| autoFocus | `boolean` | `false` | 自动聚焦 |
| tabIndex | `number` | - | Tab 键顺序 |
| data | `Record<string, any>` | - | 数据属性 |
| containerStyle | `React.CSSProperties` | - | 容器样式 |
| wrapperStyle | `React.CSSProperties` | - | 包装器样式 |
| labelStyle | `React.CSSProperties` | - | 标签样式 |
| errorTextStyle | `React.CSSProperties` | - | 错误文本样式 |
| helperTextStyle | `React.CSSProperties` | - | 辅助文本样式 |

### RadioRef

| 方法 | 类型 | 说明 |
| --- | --- | --- |
| element | `HTMLInputElement \| null` | 单选框元素 |
| getChecked | `() => boolean` | 获取选中状态 |
| setChecked | `(checked: boolean) => void` | 设置选中状态 |
| toggle | `() => void` | 切换选中状态 |
| setDisabled | `(disabled: boolean) => void` | 设置禁用状态 |
| setReadonly | `(readonly: boolean) => void` | 设置只读状态 |
| setStatus | `(status: RadioStatus) => void` | 设置单选框状态 |
| getStatus | `() => RadioStatus` | 获取单选框状态 |
| setSize | `(size: RadioSize) => void` | 设置单选框尺寸 |
| setColor | `(color: RadioColor) => void` | 设置单选框颜色 |
| validate | `() => Promise<{ valid: boolean; message?: string }>` | 验证单选框 |
| reset | `() => void` | 重置单选框 |
| getData | `() => Record<string, any> \| undefined` | 获取单选框数据 |
| setData | `(data: Record<string, any>) => void` | 设置单选框数据 |
| focus | `() => void` | 聚焦单选框 |
| blur | `() => void` | 失焦单选框 |
| shake | `() => void` | 震动效果 - 用于错误提示 |
| pulse | `() => void` | 脉冲效果 - 用于吸引用户注意 |

### RadioGroupProps

| 属性 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| children | `ReactNode` | - | 单选框组内容 |
| value | `string \| number` | - | 单选框组值 |
| defaultValue | `string \| number` | - | 默认值（非受控模式） |
| size | `RadioSize` | `'md'` | 单选框组尺寸 |
| status | `RadioStatus` | `'normal'` | 单选框组状态 |
| variant | `RadioVariant` | `'default'` | 单选框组变体 |
| color | `RadioColor` | `'primary'` | 单选框组颜色主题 |
| disabled | `boolean` | `false` | 是否禁用 |
| readonly | `boolean` | `false` | 是否只读 |
| direction | `'horizontal' \| 'vertical'` | `'horizontal'` | 布局方向 |
| align | `'start' \| 'center' \| 'end'` | `'start'` | 对齐方式 |
| spacing | `number \| string` | - | 间距 |
| options | `Array<{ label: ReactNode; value: string \| number; disabled?: boolean; description?: ReactNode; icon?: ReactNode; color?: RadioColor; data?: Record<string, any>; }>` | - | 选项列表 |
| onChange | `(selectedValue: string \| number) => void` | - | 变化事件处理函数 |
| className | `string` | - | 自定义样式类名 |
| style | `React.CSSProperties` | - | 自定义样式 |
| required | `boolean` | `false` | 是否必填 |
| compact | `boolean` | `false` | 是否紧凑布局 |
| block | `boolean` | `false` | 是否块级显示 |
| groupTitle | `ReactNode` | - | 分组标题 |
| groupDescription | `ReactNode` | - | 分组描述 |
| accessibilityLabel | `string` | - | 无障碍标签 |
| accessibilityRole | `string` | - | 无障碍角色 |
| accessibilityState | `{ disabled?: boolean; readonly?: boolean; busy?: boolean; expanded?: boolean; }` | - | 无障碍状态 |

### RadioGroupRef

| 方法 | 类型 | 说明 |
| --- | --- | --- |
| getValue | `() => string \| number \| undefined` | 获取选中值 |
| setValue | `(value: string \| number) => void` | 设置选中值 |
| clear | `() => void` | 清除选择 |
| setDisabled | `(disabled: boolean) => void` | 设置禁用状态 |
| setReadonly | `(readonly: boolean) => void` | 设置只读状态 |
| setStatus | `(status: RadioStatus) => void` | 设置组状态 |
| validate | `() => Promise<{ valid: boolean; message?: string }>` | 验证单选框组 |
| reset | `() => void` | 重置单选框组 |
| getSelectedOption | `() => { label: ReactNode; value: string \| number; disabled?: boolean; description?: ReactNode; icon?: ReactNode; color?: RadioColor; data?: Record<string, any>; } \| null` | 获取选中的选项数据 |
| getOptionByValue | `(value: string \| number) => any` | 根据值获取选项 |
| setOptionDisabled | `(value: string \| number, disabled: boolean) => void` | 根据值设置选项状态 |
| setOptionsDisabled | `(values: Array<string \| number>, disabled: boolean) => void` | 批量设置选项状态 |
| focus | `() => void` | 聚焦组 |
| blur | `() => void` | 失焦组 |

## 使用提示

1. **单选框组**：通常与 `RadioGroup` 组件一起使用，以便统一管理选中状态。

2. **受控与非受控**：通过 `checked` 属性实现受控模式，通过 `defaultChecked` 属性实现非受控模式。

3. **验证**：支持 `required`、`validator` 等验证规则，可通过 `validateTrigger` 设置验证触发时机。

4. **无障碍**：默认启用无障碍访问，可通过 `accessible` 属性关闭，或通过 `accessibilityLabel` 等属性自定义无障碍配置。

5. **自定义样式**：提供丰富的样式配置选项，包括 `style`、`className`、`labelStyle` 等。

## 浏览器兼容性

| 浏览器 | 版本 |
| --- | --- |
| Chrome | 60+ |
| Firefox | 55+ |
| Safari | 12+ |
| Edge | 79+ |

## 无障碍支持

- 支持 WAI-ARIA 标准
- 键盘导航支持
- 屏幕阅读器支持
- 语义化标签